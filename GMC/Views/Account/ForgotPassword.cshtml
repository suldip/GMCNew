@model GMC.Models.GMC.ForgotPasswordModel

@{
    ViewData["Title"] = "Forgot Password";
    Layout = null; // Standalone layout
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forgot Password - GMC</title>
    <partial name="_InterFont" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: "Inter", system-ui, -apple-system, "Segoe UI", sans-serif;
            background-color: #f8f9fa; /* Light grey background */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .auth-card {
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.06);
            width: 100%;
            max-width: 450px;
            padding: 3rem;
        }

        .auth-header h1 {
            font-weight: 800;
            color: #111;
            margin-bottom: 0.5rem;
            font-size: 2rem;
        }

        .auth-header p {
            color: #666;
            margin-bottom: 2.5rem;
            font-size: 0.95rem;
        }

        .back-link {
            display: inline-block;
            margin-bottom: 2rem;
            color: #555;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .back-link:hover {
            color: #111;
        }

        .form-label {
            font-weight: 600;
            color: #333;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .form-control-icon-wrapper {
            position: relative;
        }

        .form-control-icon-wrapper i {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: #999;
            font-size: 1.1rem;
        }

        .form-control-with-icon {
            padding-left: 2.8rem;
            height: 48px;
            border-radius: 6px;
            border: 1px solid #ddd;
            font-size: 0.95rem;
        }
        
        .form-control:focus {
            border-color: #0b5ed7;
            box-shadow: 0 0 0 0.25rem rgba(11, 94, 215, 0.1);
        }

        .btn-primary-auth {
            height: 48px;
            font-weight: 600;
            font-size: 1rem;
            border-radius: 6px;
            background-color: #0d6efd;
            border: none;
            width: 100%;
            color: white;
            transition: all 0.2s ease-in-out;
            margin-top: 1.5rem;
        }

        .btn-primary-auth:hover {
            background-color: #0b5ed7;
        }

        .bg-error {
            background-color: #f8d7da;
            color: #842029;
            padding: 1rem;
            border-radius: 6px;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            border: 1px solid #f5c2c7;
        }
    </style>
</head>
<body>

    <div class="auth-card">
        <a asp-controller="Account" asp-action="Login" class="back-link">
            <i class="bi bi-arrow-left me-2"></i> Back to login
        </a>

        <div class="auth-header">
            <h1>Forgot password?</h1>
            <p>Enter your email and we'll send you a reset link</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="bg-error">
                <i class="bi bi-exclamation-triangle-fill me-2"></i> @ViewBag.Error
            </div>
        }

        <form asp-action="ForgotPassword" method="post">
            <div class="mb-4">
                <label asp-for="EmailAddress" class="form-label">Email address</label>
                <div class="form-control-icon-wrapper">
                    <i class="bi bi-envelope"></i>
                    <input asp-for="EmailAddress" class="form-control form-control-with-icon" placeholder="name@company.com" required />
                </div>
                <span asp-validation-for="EmailAddress" class="text-danger small mt-1 d-block"></span>
            </div>

            <button type="submit" class="btn btn-primary-auth">
                Send reset link
            </button>
        </form>
    </div>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
</body>
</html>
