@model GMC.Models.GMC.ResetPasswordModel

@{
    ViewData["Title"] = "Reset Password";
    Layout = null; // Standalone layout
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Set new password - GMC</title>
    <partial name="_InterFont" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: "Inter", system-ui, -apple-system, "Segoe UI", sans-serif;
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .auth-card {
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.06);
            width: 100%;
            max-width: 450px;
            padding: 3rem;
        }

        .auth-header h1 {
            font-weight: 800;
            color: #111;
            margin-bottom: 0.5rem;
            font-size: 2rem;
        }

        .auth-header p {
            color: #666;
            margin-bottom: 2.5rem;
            font-size: 0.95rem;
        }

        .form-label {
            font-weight: 600;
            color: #333;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .form-control-icon-wrapper {
            position: relative;
        }

        .form-control-icon-wrapper i {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: #999;
            font-size: 1.1rem;
        }

        .form-control-with-icon {
            padding-left: 2.8rem;
            height: 48px;
            border-radius: 6px;
            border: 1px solid #ddd;
            font-size: 0.95rem;
        }
        
        .form-control:focus {
            border-color: #0b5ed7;
            box-shadow: 0 0 0 0.25rem rgba(11, 94, 215, 0.1);
        }

        .btn-primary-auth {
            height: 48px;
            font-weight: 600;
            font-size: 1rem;
            border-radius: 6px;
            background-color: #0d6efd;
            border: none;
            width: 100%;
            color: white;
            transition: all 0.2s ease-in-out;
            margin-top: 1.5rem;
        }

        .btn-primary-auth:hover {
            background-color: #0b5ed7;
        }

        .bg-error {
            background-color: #f8d7da;
            color: #842029;
            padding: 1rem;
            border-radius: 6px;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            border: 1px solid #f5c2c7;
        }
    </style>
</head>
<body>

    <div class="auth-card">
        <div class="auth-header">
            <h1>Set new password</h1>
            <p>Your new password must be different to previously used passwords.</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="bg-error">
                <i class="bi bi-exclamation-triangle-fill me-2"></i> @ViewBag.Error
            </div>
        }

        <form asp-action="ResetPassword" method="post">
            <input type="hidden" asp-for="EmailAddress" />

            <div class="mb-4">
                <label asp-for="NewPassword" class="form-label">New Password</label>
                <div class="form-control-icon-wrapper">
                    <i class="bi bi-lock"></i>
                    <input asp-for="NewPassword" type="password" class="form-control form-control-with-icon" placeholder="••••••••" required />
                </div>
                <span asp-validation-for="NewPassword" class="text-danger small mt-1 d-block"></span>
            </div>

            <div class="mb-4">
                <label asp-for="ConfirmPassword" class="form-label">Confirm Password</label>
                <div class="form-control-icon-wrapper">
                    <i class="bi bi-lock-fill"></i>
                    <input asp-for="ConfirmPassword" type="password" class="form-control form-control-with-icon" placeholder="••••••••" required />
                </div>
                <span asp-validation-for="ConfirmPassword" class="text-danger small mt-1 d-block"></span>
            </div>

            <button type="submit" class="btn btn-primary-auth">
                Reset password
            </button>
        </form>
    </div>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
</body>
</html>
